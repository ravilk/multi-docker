{
    "AWSEBDockerrunVersion": 2,
    "containerDefinitions": [
        {
            "name": "client",                       /* Set name for our container */
            "image": "ravilkhalilov/multi-client",  /* Set docker image that will be pulled from your DockerHub account */
            "hostname": "client",
            "essential": false                      /* If this container os failed other containers should not be shutdown at the same time */
        },
        {
            "name": "server",                       /* Set name for our container */
            "image": "ravilkhalilov/multi-server",  /* Set docker image that will be pulled from your DockerHub account */
            "hostname": "api",
            "essential": false                      /* If this container os failed other containers should not be shutdown at the same time */
        },
        {
            "name": "worker",                       /* Set name for our container */
            "image": "ravilkhalilov/multi-worker",  /* Set docker image that will be pulled from your DockerHub account */
            "hostname": "worker",
            "essential": false                      /* If this container os failed other containers should not be shutdown at the same time */
        },
        {
            "name": "nginx",                        /* Set name for our container */
            "image": "ravilkhalilov/multi-nginx",   /* Set docker image that will be pulled from your DockerHub account */
            "hostname": "nginx",
            "essential": true,                      /* If this container os failed other containers should be shutdown at the same time */
            "portMappings": [
                {
                    "hostPort": 80,
                    "containerPort": 80
                }
            ],
            "links": ["client","server"]
        }
    ]
}
